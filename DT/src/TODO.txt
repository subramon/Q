* Consider micro-benchmark for how much better reorder is over sort
   remove benchmark_sort.c because it is incorporated in ut_reorde.c
* How do we mark a feature as not worth splitting?
* How do we know whether search produced anything at all?
* Convert config file to use Lua and a real config file 
  (see lustre/benchmark)
