- Modification in pow() operation
  User can provide qtype in optargs, use user specified qtype as qtype for out_vector.
  If not specified then use in_vector qtype for out_vector

-- logit2
 do common sub-expression elimination
