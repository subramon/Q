-- no of columns not same on each csv line
-- in the corresponding csv file 
-- second row contains only one column value instead of two
return { 
  { name = "col1", qtype = "I4", has_nulls =true },
  { name = "col2", qtype ="I2" }
}