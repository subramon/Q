This directory contains files which get modified while trying out the Q code on GPU using CUDA.

We were just trying out single operator i.e F1F2OPF3
We did successfully run the F1F2OPF3 test on GPU machine.

file name contains the path where these files needs to be situated.
